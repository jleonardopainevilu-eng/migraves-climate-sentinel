{
  "name": "migraves-climate-sentinel",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Research-support prototype that creates traceable Migratory Bioclimatic Records from bird observations, climate data, and human validation.",
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check app.js && node --check api/analyze.js",
    "serve": "npx serve .",
    "test": "node tests/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vercel": "^46.0.0"
  }
}
